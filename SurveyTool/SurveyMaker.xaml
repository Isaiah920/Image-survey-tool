<Window x:Class="SurveyTool.SurveyMaker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SurveyMaker" Height="550" Width="800" Loaded="Window_Loaded">
    <Grid>
        <ScrollViewer Grid.Row="1" HorizontalAlignment="Stretch" Name="scrollViewer1" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto" Margin="5">
            <Grid Name="MainGrid">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Button Content="Button" Grid.Row="1" Height="23" HorizontalAlignment="Right" Margin="0,14,15,0" Name="button1" VerticalAlignment="Top" Width="75" />
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
